apiVersion: v1
kind: Service
metadata:
  name: prescription-service
  labels:
    app: prescription-service
spec:
  type: ClusterIP
  ports:
  - port: 5004
    targetPort: 5004
  selector:
    app: prescription-service
